# 리뷰 — 기획 문서 일체 (2026-09-12)

**리뷰 대상**: `docs/BRIEF.md` · `docs/requirements.md` · `docs/open-questions.md` · `docs/research/market.md` · `docs/ux/flows.md` · `docs/architecture.md` · `docs/README.md` · `AGENTS.md` · `CLAUDE.md`

**작성 모델**: `requirements.md`·`architecture.md`는 opus(pm·architect)가 썼다. `research/market.md`·`ux/flows.md`는 **sonnet — 이 리뷰를 쓰는 모델과 같다.** 이 두 문서에 대해서는 작성자-검증자 오류 비상관성이 약해진다는 점을 감안하고 읽을 것. (`open-questions.md`·`BRIEF.md`는 CLAUDE.md 역할표 기준 pm=opus, `README.md`는 scribe=haiku 작성으로 추정.)

**지적 총 개수**: H 3건 · M 5건 · L 3건 (총 11건)

작성 에이전트의 대화나 보고서는 참고하지 않았다. 문서 본문만 보고 판단했다.

| # | 유형(1~7) | 파일:라인 | 문제 | 심각도 |
|---|---|---|---|---|
| 1 | 1 | `requirements.md:105` ↔ `architecture.md:196-199,213,215` | DC-R08은 "확인된 기록이 3건 미만이면 ... '아직 취향을 모른다'를 명시한다"고 무조건 규정한다. 그러나 architecture.md 4.5 `buildReason`은 count<3이어도 맥락 규칙(season/weekday/late)이 1개 이상 매칭되면 그 규칙의 문구(예: "요즘 계절에 맞는 걸로 골랐다")를 대신 반환하고, 4.6은 "맥락 규칙이 1개 이상이면 그 문구"이며 "이 셋은 기록 0건이어도 날짜와 시각만으로 계산된다"고 명시한다 — 즉 콜드스타트에서 맥락 규칙이 거의 매일 걸리므로, DC-R08이 요구하는 "아직 취향을 모른다"는 실제로는 거의 노출되지 않는다. | H |
| 2 | 1 | `architecture.md:213,215` ↔ `ux/flows.md:20-23` | architecture 4.6은 콜드스타트 이유 줄이 "맥락 규칙 1개 이상이면 그 문구"로 갈린다고 하는데, ux/flows.md 흐름(a) 3단계는 조건 분기 없이 "이유줄='아직 취향을 몰라서 오늘은 무작위로 골랐다'"로 매번 고정해 그린다. 같은 파이프라인을 두 문서가 다르게 그린다. | M |
| 3 | 1 | `requirements.md:60,125` ↔ `ux/flows.md:54-65, 112-172` | MO-R05는 해 먹기(A) 결정의 카드에 "집에 재료가 있다는 전제"를 명시하라고 요구하고, requirements 3.3 A행은 이걸 "→ MO-R05"로 메운 구멍이라 부른다. 그런데 ux/flows.md의 DC 출력 정의(3절)·문구 표(5절)·와이어프레임(4절, 해먹기 사례 "된장찌개 · 해 먹기" 포함)을 다 봐도 "재료"라는 단어 자체가 한 번도 나오지 않는다(`grep -n "재료" docs/ux/flows.md` 결과 0건). 요구한 고지가 화면 설계에 전혀 없다. | H |
| 4 | 1 | `open-questions.md:42` ↔ `requirements.md:103,151` / `ux/flows.md:62,204` | DC-OQ-04 추천 문구는 "지금은 정해줄 수 없어요"(해요체)인데, requirements DC-R06·6.6 표와 ux/flows.md 문구 표·상태 목록은 전부 "지금은 정해줄 수 없다"(평서체)로 적었다. 6.6이 "모든 화면이 같은 말을 쓴다"고 못박은 고정 문구 자체가 두 어미로 갈린다. | L |
| 5 | 1 | `requirements.md:145-154(6.6)` ↔ `requirements.md:131(PR-R03)` | 6.6은 "빈 값 = 아직 데이터가 만들어진 적 없음 → '아직 기록이 없다'"로 6개 상태에 화면 공통 문구를 하나씩 못박는다("모든 화면이 같은 말을 쓴다"). 그런데 PR의 빈 프로필(=아직 만들어진 적 없는 데이터, 개념상 "빈 값")은 PR-R03에서 별도로 "아직 입력 안 함"을 쓰라고 정해 6.6표의 "빈 값" 문구와 다른 말을 쓴다. 6.6 어디에도 PR이 예외라는 언급이 없다. | M |
| 6 | 1 | `ux/flows.md:149-152` ↔ `architecture.md:138-147` | DC-R07 "후보 0개" 요구를 구현한 ux 와이어프레임은 "절대 제외에 걸림/예산 초과/최근 14일 안에 먹음/조리 불가" 네 항목을 보여준다. 그러나 architecture.md 4.2의 실제 필터 파이프라인이 집계하는 `blocked.*`는 `branch`·`absolute`·`todayReject`·`recent` 네 개뿐이다 — 예산(`budgetMaxKrw`)과 조리 가능 여부(`canCook`)는 필터가 아니라 점수 가감(150-161행, `s+=8`/`s+=canCook?8:-1000`)일 뿐 걸러진 개수를 세지 않는다. 와이어프레임의 "예산 초과 8개", "조리 불가 3개"는 이 알고리즘으로는 애초에 계산할 수 없는 숫자다. | H |
| 7 | 2 | `requirements.md:121` cf. `requirements.md:123` / `requirements.md:129` cf. `requirements.md:130,132` | MO-R01("갈래는 정확히 3개다... 목록은 화면에서 줄지 않는다")과 PR-R01("저장 항목은 다섯 가지다: ...")은 에이전트가 임의로 정한 UI·데이터 구조 결정인데 아무 태그가 없다. 바로 옆 MO-R03·PR-R02·PR-R04는 같은 성격의 결정에 `[제안]`을 붙였다 — 같은 절 안에서 태깅 기준이 일관되지 않는다. (같은 패턴이 RE-R01~03, HI-R01~04·06 등 6절 전반에 반복된다.) | M |
| 8 | 2 | `ux/flows.md:44` | 흐름(d) 3단계 "오늘 남은 재시도는 없음 — 화면은 새 갈래로 바꾸는 것(MO)까지는 막지 않음"은 어떤 R-ID나 `[제안]` 태그도 없이 새로 만들어낸 동작 규칙이다(거절 상한이 갈래 전환은 막지 않는다는 결정을 이 문서가 처음 내린다). | L |
| 9 | 3 | `requirements.md:125` | MO-R05 한 문장 안에서 "카드에 '집에 재료가 있다는 전제'를 명시한다"(전제를 문장으로 진술하라)와 "재료가 있다고 단정해 쓰지 않는다"(그렇게 진술하지 말라)가 같은 대상(재료 보유 문구)에 상반된 지시를 내린다. 화면에 실제로 어떤 문장이 뜨면 통과이고 어떤 문장이 뜨면 위반인지 이 문장만으로는 가릴 수 없다. | M |
| 10 | 5 | `requirements.md:129(PR-R01)` ↔ `architecture.md:73,132-171` | PR-R01은 "같이 먹는 인원"을 취향·제약 5항목 중 하나로 저장하게 하고, architecture.md도 `Profile.partySize`로 데이터 모델에 넣었다(73행). 그러나 추천 로직 `decide()`(132-171행, 필터 138-147행·점수 150-161행)의 어디에도 `partySize`를 읽는 코드가 없다 — 나머지 네 항목(absoluteExclusions·dislikes·budgetMaxKrw·canCook)은 전부 필터·점수에 쓰이는데 이 항목만 수집되고 버려진다. PR 화면은 이 값이 오늘의 결정에 반영되는 제약처럼 보이지만 실제로는 아무 영향이 없다. | M |
| 11 | 7 | `requirements.md:116(RE-R05)` ↔ `open-questions.md:33(DC-OQ-03),72(RE-OQ-01)` | RE-R05는 "상한에 닿았을 때 화면이 쓰는 문구도 이 요구사항에 포함된다"면서 `[미정 DC-OQ-03]`만 인용한다. 그러나 DC-OQ-03의 범위(open-questions.md:33)는 "재방문 시 고정/재계산 여부 + 하루 거절 상한 횟수"까지고, 문구 자체를 다루는 전용 질문은 별도로 존재하는 RE-OQ-01(open-questions.md:72, 제목 그대로 "RE-R05 상한 도달 시 정확한 문구")이다. requirements.md 본문이 정작 관련 ID(RE-OQ-01)를 인용하지 않는다. | L |

유형 4(빠진 상태)와 유형 6(범위 이탈)은 **없음** — `ux/flows.md` 3절이 화면마다 6개 상태(빈 값/로딩/실패/후보 0개/표본 부족/미확정)를 문구로 채우거나 "해당 없음 + 6절 요구사항 제안"으로 명시적으로 추적했고, `architecture.md`도 위치·재고·소셜·계정 등 3.2 범위 밖 항목을 스스로 배제하는 근거를 매 절에서 밝혔다. 유형 7의 BLOCKER(21)/LATER(10) 집계는 `requirements.md` 8절과 `open-questions.md`가 ID 단위로 정확히 일치한다(교차 대조 완료) — 위 11번 외에 개수·ID 불일치는 찾지 못했다.

---

리뷰어가 고칠 수 없는 것 — 고칠지는 사람이 정한다.
