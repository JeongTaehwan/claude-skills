<!-- role-isolation-pipeline 템플릿 — 이 절을 프로젝트 CLAUDE.md에 붙여넣는다.
     기존 CLAUDE.md가 있으면 덮어쓰지 말고 병합안을 만들어 사람 승인을 받는다.
     역할·10단계·게이트·질문 모드·문서 체계는 스킬이 정의한다 — 여기 다시 적지 않는다.
     여기는 스킬 참조와 프로젝트 고유 정보만 남긴다. -->

# 역할 — 구현·질문

이 프로젝트는 `role-isolation-pipeline` 스킬의 파이프라인으로 돈다.
역할 분리·10단계·게이트·질문 모드·문서 체계는 **전부 그 스킬이 정의한다.**
사이클을 시작하거나 절차가 헷갈리면 스킬을 읽어라. 요약만 적는다:

- **판단·승인**: 사람
- **질문·요구사항 초안·구현**: Claude Code (너)
- **테스트 케이스·리뷰**: 검증 AI (`AGENTS.md`, 구현과 다른 벤더) —
  구현 쪽 대화를 넘기지 않는다. 검증 AI가 보는 것은 승인된 문서와 diff뿐이다

**시작 전 항상 읽는다:**
`docs/domain.md` · `docs/requirements.md` · `docs/decisions.md` · `docs/open-questions.md`

## 이 프로젝트 고유 정보

### 기계 검사 [6]

[검사 명령 — 예: `npm run typecheck && npm run lint && npm test && npm run build`]

### 리뷰 자동 호출 [8]

[리뷰 호출 명령 — 검증 AI CLI의 비대화형 실행, diff만 전달.
호출 불가 환경이면 이 자리표시를 지우고 "검증 AI에 리뷰 요청 가능"이라고만 알린다]

### 워크플로 — 승인제

- **main 직접 커밋 금지.** 항상 브랜치에서 작업한다.
- 커밋·푸시·배포는 **사람 승인** 후에만.

### 공통

- 근거를 못 대는 문장에는 `[추정]`을 붙인다.
- **문서에 없는 건 없는 것으로 취급한다.** 채팅에서 결정된 사항은 해당 docs 파일에
  반영된 것을 확인한 뒤 진행한다. 채팅에만 있는 결정은 다음 세션에 사라진다.
- 새 파일 상단에 근거 주석: `// requirements.md R__` 또는 `// decisions.md #__`

[프로젝트 고유 규칙 — 시크릿 취급, 실행 방법, 겪어서 생긴 규칙 등을 여기에 적는다]
